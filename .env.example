# Copy this file to .env (which is gitignored) and fill in your key.
# Loaded automatically by any shell-level `source .env` or by tools that read .env.
# The Python code reads only from the process environment, never from disk.

OPENROUTER_API_KEY=sk-or-v1-REPLACE_ME
# Optional: override the default base URL (defaults to https://openrouter.ai/api/v1).
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
